{
  "name": "copilot-dev-assistant",
  "displayName": "Copilot Dev Assistant",
  "description": "AI Dev Assistant that extends GitHub Copilot Chat with custom slash commands, modular agents, skills, rules, and prompt templates — all driven by markdown files.",
  "version": "1.0.0",
  "publisher": "your-publisher-id",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/copilot-dev-assistant"
  },
  "engines": {
    "vscode": "^1.93.0"
  },
  "categories": [
    "Chat"
  ],
  "keywords": [
    "copilot",
    "chat",
    "ai",
    "agent",
    "assistant"
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "chatParticipants": [
      {
        "id": "copilot-dev-assistant.agent",
        "name": "agent",
        "fullName": "Dev Assistant",
        "description": "AI Dev Assistant with specialized sub-agents for backend, testing, code review, and security.",
        "isSticky": true,
        "commands": [
          {
            "name": "generate-test",
            "description": "Generate comprehensive tests for the active file"
          },
          {
            "name": "review",
            "description": "Perform a thorough code review with actionable feedback"
          },
          {
            "name": "new-module",
            "description": "Scaffold a new module following project architecture"
          },
          {
            "name": "secure-check",
            "description": "Audit code for security vulnerabilities"
          }
        ]
      }
    ]
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "lint": "tsc --noEmit",
    "package": "npm run compile && npx @vscode/vsce package --no-dependencies"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.93.0",
    "typescript": "^5.4.0",
    "@vscode/vsce": "^3.0.0"
  }
}
